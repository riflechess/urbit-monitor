use urbit-http-api::{AuthoredMessage, Chatbot, Message};

fn main() {
  //let chat_bot = Chatbot::new_with_local_config(respond_to_message, "~sitsev-lomrem", "chat-6301");
  println!("Staring urbit monitor.");
}